# AI usage — development methodology

> **Write this yourself, in your own words.** The assessment asks for a transparent,
> manually written account of how AI tools were used and when they entered your workflow.
> It does not need to be polished — it needs to be honest and specific. The prompts below
> are only a checklist; delete them as you write.

## Which tools I used

<!-- Name the tool(s) and roughly which parts of the work they touched. -->

## When in the lifecycle I used them

<!-- Be concrete about the stages:
     - planning and architecture decisions
     - scaffolding the repos
     - writing feature code
     - writing tests
     - debugging
     - documentation -->

## What I decided myself

<!-- The architecture calls were yours: NestJS over Express, separate repos over a
     monorepo, modular monolith, repository pattern, refresh-token rotation with reuse
     detection, separate admin frontend, RBAC. Say which decisions you drove and why. -->

## How I verified the output

<!-- How did you check the generated code actually worked?
     - type checking and linting
     - unit and e2e test suites
     - running the stack in Docker and clicking through it
     - reading the diffs before committing -->

## What I changed or rejected

<!-- Anything the tool suggested that you corrected, simplified, or threw away. This is
     the most useful part of the doc for a reviewer — be specific. -->

## What I would do differently

<!-- Honest reflection. -->

---

**Note on the docs:** the per-domain rationale docs (`DATABASE.md`, `BACKEND.md` in the
backend repo, `FRONTEND.md` in the client repo) and this file are written manually, as the
assessment requires.
